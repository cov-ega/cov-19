import { SimplePipePipe } from './simple-pipe.pipe';

describe('SimplePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SimplePipePipe();
    expect(pipe).toBeTruthy();
  });
});
